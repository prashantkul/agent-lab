[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers =
    admin: Admin portal tests
    reviewer: Reviewer portal tests
    student: Student portal tests
    slow: Tests that take longer to run
